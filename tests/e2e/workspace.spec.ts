import {test,expect} from '@playwright/test';
import AxeBuilder from '@axe-core/playwright';
test('workspace loads and fits the viewport',async({page})=>{await page.goto('/preview/ask');await expect(page.getByRole('heading',{name:'What does your building need to know?'})).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1)).toBe(true);const a11y=await new AxeBuilder({page}).analyze();expect(a11y.violations.filter(v=>v.impact==='critical'||v.impact==='serious')).toEqual([]);});
test('agent configuration is accessible and sample writes are clearly blocked',async({page})=>{await page.goto('/preview/agents');await page.getByRole('button',{name:'Create agent',exact:true}).click();await page.getByLabel('Agent name').fill('Test agent');await page.getByRole('button',{name:'Save changes'}).click();await expect(page.getByRole('alert')).toContainText('sample workspace');});
test('citation opens the exact supporting passage',async({page})=>{await page.goto('/preview/conversation');await page.getByRole('button',{name:'Open source 1'}).first().click();await expect(page.getByRole('dialog')).toContainText('Visitor parking spaces are reserved for guests');await expect(page.getByRole('dialog')).toContainText('2025-03-12');});
test('auth has required labels and leaves role selection out of login',async({page})=>{await page.goto('/login');await expect(page.getByRole('heading',{name:'Welcome back.'})).toBeVisible();await expect(page.getByLabel('Email address')).toBeVisible();await expect(page.getByLabel('Password',{exact:true})).toBeVisible();});
